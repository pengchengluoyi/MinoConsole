# Leave empty in production if Console is reverse-proxied onto the same origin as Nexus.
# Local / LAN default is the mDNS name Nexus registers: http://mino.local:10104
VITE_NEXUS_URL=
VITE_MINO_CLIENT=console
